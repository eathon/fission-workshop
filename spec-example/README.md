# Fission Specs

* Create application

```
$ fission spec init
$ fission create --spec ...
```

* Deploy

```
$ fission validate
$ fission apply --wait
```

* Live-Reload(Test)
```
$ fission apply --watch 
```

